@startuml

class           Segment {
  + SegmentId getId()
  + SegmentResult get()
  + SegmentResult put()
  + EntryIterator openIterator()
  + SegmentResult flush()
  + SegmentResult compact()
  + long getNumberOfKeys()
  + void close()
}

note right of Segment
  Public contract for a single
  durable segment.
end note

class           SegmentId {
  - int id  
  + int getId() 
}
class           SegmentPropertiesManager
class           SegmentCompacter
class           SegmentFiles
class           SegmentConf

Segment --> SegmentPropertiesManager
Segment --> SegmentCompacter
Segment --> SegmentFiles
Segment --> SegmentConf

@enduml
