name: Maven Build, test and Verify

on:
  push:
    branches-ignore: [ "main" ]
  pull_request:
    branches-ignore: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    steps:
    
    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Show default environment variables
      run: |
        echo "The job_id is: $GITHUB_JOB"   # reference the default environment variables
        echo "The id of this action is: $GITHUB_ACTION"   # reference the default environment variables
        echo "The run id is: $GITHUB_RUN_ID"
        echo "The GitHub Actor's username is: $GITHUB_ACTOR"
        echo "GitHub SHA: $GITHUB_SHA"
        echo "github.ref_name: ${{ github.ref_name }}"
        echo "github.ref: ${{ github.ref }}"

    - name: Cache local Maven repository
      uses: actions/cache@v5
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-        

    - name: Prepare Dependency-Check cache settings
      if: github.event_name != 'pull_request'
      id: dependency-check-cache
      shell: bash
      run: |
        echo "path=$HOME/.cache/dependency-check" >> "$GITHUB_OUTPUT"
        echo "key=${{ runner.os }}-dependency-check-data-v1-$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"

    - name: Restore Dependency-Check data
      if: github.event_name != 'pull_request'
      id: dependency-check-cache-restore
      uses: actions/cache/restore@v5
      with:
        path: ${{ steps.dependency-check-cache.outputs.path }}
        key: ${{ steps.dependency-check-cache.outputs.key }}
        restore-keys: |
          ${{ runner.os }}-dependency-check-data-v1-

    - name: Show NVD API key availability
      shell: bash
      run: |
        if [ -n "${NVD_API_KEY:-}" ]; then
          echo "NVD_API_KEY is set"
        else
          echo "NVD_API_KEY is missing"
        fi

    - name: Build with Maven (PR)
      if: github.event_name == 'pull_request'
      run: mvn -B -Ddependency-check.skip=true verify --file pom.xml

    - name: Build with Maven
      if: github.event_name != 'pull_request'
      run: mvn -B verify --file pom.xml

    - name: Save Dependency-Check data
      if: always() && github.event_name != 'pull_request' && !cancelled() && steps.dependency-check-cache-restore.outputs.cache-hit != 'true'
      uses: actions/cache/save@v5
      with:
        path: ${{ steps.dependency-check-cache.outputs.path }}
        key: ${{ steps.dependency-check-cache.outputs.key }}

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      if: github.event_name != 'pull_request' && github.actor != 'dependabot[bot]'
      uses: advanced-security/maven-dependency-submission-action@v5

  wal-tools-smoke:
    name: WAL Tools Smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Cache local Maven repository
      uses: actions/cache@v5
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Build WAL tools distribution
      run: mvn -B -pl engine,wal-tools -am -DskipTests package --file pom.xml

    - name: Smoke test WAL tools scripts
      shell: bash
      run: |
        set -euo pipefail

        WAL_TOOLS_ZIP="$(ls wal-tools/target/wal-tools-*.zip | head -n1)"
        TMP_DIR="$(mktemp -d)"
        trap 'rm -rf "${TMP_DIR}"' EXIT

        unzip -q "${WAL_TOOLS_ZIP}" -d "${TMP_DIR}"
        DIST_DIR="$(find "${TMP_DIR}" -maxdepth 1 -mindepth 1 -type d -name 'wal-tools-*' | head -n1)"

        set +e
        "${DIST_DIR}/bin/wal_verify" >"${TMP_DIR}/verify-no-args.out" 2>&1
        VERIFY_NO_ARGS_EXIT=$?
        "${DIST_DIR}/bin/wal_dump" >"${TMP_DIR}/dump-no-args.out" 2>&1
        DUMP_NO_ARGS_EXIT=$?
        "${DIST_DIR}/bin/wal_verify" /tmp/hestia-missing-wal >"${TMP_DIR}/verify-missing-dir.out" 2>&1
        VERIFY_MISSING_DIR_EXIT=$?
        set -e

        test "${VERIFY_NO_ARGS_EXIT}" -eq 1
        test "${DUMP_NO_ARGS_EXIT}" -eq 1
        test "${VERIFY_MISSING_DIR_EXIT}" -eq 1

        grep -q "Usage: wal_verify" "${TMP_DIR}/verify-no-args.out"
        grep -q "Usage: wal_dump" "${TMP_DIR}/dump-no-args.out"
        grep -q "WAL directory does not exist" "${TMP_DIR}/verify-missing-dir.out"
