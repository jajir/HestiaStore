@startuml

class SegmentIndex<K, V> {
  + void put(Entry<K, V> entry)
  + put(K key, V value)
  + V get(K key);
  + void delete(K key);
  + void compact();
  + void flush();
  + Stream<Entry<K, V>> getStream();
}

@enduml