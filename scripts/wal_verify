#!/usr/bin/env bash
set -euo pipefail

readonly SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"

if [[ $# -lt 1 ]]; then
  echo "Usage: wal_verify <walDirectoryPath> [--json]"
  exit 1
fi

exec "${SCRIPT_DIR}/wal_tool.sh" verify "$@"
